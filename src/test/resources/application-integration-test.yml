spring:
  datasource:
    url: jdbc:h2:mem:reservisland;MODE=Oracle
    username: sa
    password:
    driverClassName: org.h2.Driver
    liquibase:
      jdbcUrl: jdbc:h2:mem:reservisland;MODE=Oracle
      username: sa
      password:
      driverClassName: org.h2.Driver
